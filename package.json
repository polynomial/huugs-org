{
  "name": "huugs-media",
  "version": "1.0.0",
  "description": "Huugs Media Photography Collection",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:clean": "pkill -f 'node server.js' || true && sleep 1 && node server.js",
    "process-images": "node scripts/process-images.js",
    "test": "node scripts/test-gallery.js",
    "test:masonry": "node scripts/test-masonry.js",
    "build": "cp -r public dist"
  },
  "dependencies": {
    "sharp": "^0.33.2",
    "glob": "^10.3.10",
    "mkdirp": "^3.0.1"
  },
  "devDependencies": {
    "puppeteer": "^22.0.0"
  }
}
